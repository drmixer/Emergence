# Frontend environment variables
# Local development can omit these and use the default localhost backend.
# Production deploys should set both values explicitly.

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000
